#include "head.h"
#include "LengthOfLongestSubstring.h"
#include "FindMedianSortedArrays.h"
#include "LongestPalindrome.h"
#include "ZNConvert.h"
#include "ThreeSum.h"
#include "LongestCommonPrefix.h"
#include "RemoveNthFromEnd.h"
#include "GenerateParenthesis.h"

int main() {
	//LengthOfLongestSubstring sl;
	//string s("abcabcbb");
	//int i = sl.lengthOfLongestSubstring(s);
	//cout << i << endl;

	//vector<int> n1, n2;
	//n1.push_back(1);
	//n1.push_back(3);
	////n2.push_back(2);
	//FindMedianSortedArrays f;
	//double a = f.findMedianSortedArrays(n1, n2);
	//cout << a << endl;

	//LongestPalindrome lp;
	//string s("tattarrattat");
	//cout << lp.longestPalindrome(s).c_str() << endl;

	//ZNConvert cv;
	//string s("PAYPALISHIRING");
	//cout << cv.convert(s, 4).c_str() << endl;

	//int arr[] = { -4,-2,-2,-2,0,1,2,2,2,3,3,4,4,6,6 };
	//ThreeSum ts;
	//vector<int> nums(arr, arr+15);
	//auto v = ts.threeSum(nums);

	//string arr[] = { "dog","racecar","car"};
	//vector<string> strs(arr, arr + 3);
	//LongestCommonPrefix lcp;
	//cout << lcp.longestCommonPrefix(strs).c_str() << endl;

	//ListNode * head = new ListNode(1);
	//head->next = new ListNode(2);
	//head->next->next = new ListNode(3);
	//head->next->next->next = new ListNode(4);
	//head->next->next->next->next = new ListNode(5);
	//RemoveNthFromEnd rnfe;
	//ListNode * p = rnfe.removeNthFromEnd(head, 2);

	//GenerateParenthesis gpt;
	//vector<string> v_s = gpt.generateParenthesis(3);

	system("pause");
	return 0;
}